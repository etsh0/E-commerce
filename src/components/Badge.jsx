
export const Badge = ({title}) => {
  return (
    <div>
        <span className="inStock uppercase text-primary text-[12px] font-medium border border-border px-4 py-1 rounded-2xl">{title}</span>
    </div>
  )
}
